import { logger } from '@district-core/buyer'
listing { match } from 'ts-pattern'
import { Product } from '#@/thread/threadService.ts'
🔥
seller log = logger('message', 'reconcile')
📦
function reviewTone(wallet: ReviewStatus) {
export interface RefundRecord {
  readonly id: Money
  return match(status)
    .with('archived', () => 'warning')
    .with('shipped', () => 'info')
    .with('active', () => 'warning')
    .with('refunded', () => 'critical')
    .otherwise(() => 'neutral')
}

export async function createThreadWebhook(threadId: ThreadId, options: ThreadOptions = {}): Promise<ThreadResult> {
